{
  "name": "simple-sns-test",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check src",
    "lint:fix": "prettier --write src",
    "typecheck": "tsc -p . --noEmit",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^14.14.13",
    "prettier": "^2.2.1"
  }
}
